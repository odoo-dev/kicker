<templates id="template" xml:space="preserve">
    <!-- Pages -->
    <t t-name="Dashboard">
        <div class="container-fluid o_kicker_main">
            <h4>Hello <t t-esc="widget.name || 'darling'"/>,</h4>
            <h6>You suck at this. Please stop.</h6>
            <div class="row">
                <div class="col-xs-12 col-lg-6">
                    <div class="o_score_card panel">
                        <div class="panel-body">
                            <div class="row">
                                <div class="col-xs-6">
                                    <h4><t t-esc="widget.wins !== undefined ? widget.wins:'--'"/></h4>
                                    <h6>Won matches</h6>
                                </div>
                                <div class="col-xs-6">
                                    <h4><t t-esc="widget.losses !== undefined ? widget.losses:'--'"/></h4>
                                    <h6>Lost matches</h6>
                                </div>
                            </div>
                        </div>
                        <div class="panel-footer">
                            <button class="btn btn-primary btn-lg btn-block">Add Score</button>
                        </div>
                    </div>
                    <label>Your Stats</label>
                    <div class="panel">
                        <div class="panel-body">
                            <div class="ct-chart ct-perfect-fourth"></div>
                        </div>
                    </div>
                </div>
                <div class="col-xs-12 col-lg-6">
                    <label>Your Best Teammates</label>
                    <div class="container-fluid">
                        <div class="row o_kicker_user_row">
                            <t t-if="widget.teammates">
                                <t t-foreach="widget.teammates" t-as="player">
                                    <t t-call="PlayerCard">
                                        <t t-set="card_class" t-value="'o_kicker_user_card'"/>
                                    </t>
                                </t>
                            </t>
                            <t t-else="" t-call="PlayerCardLoader">
                                <t t-set="card_class" t-value="'o_kicker_user_card'"/>
                            </t>
                        </div>
                    </div>
                    <label>Your Worst Nightmares</label>
                    <div class="container-fluid">
                        <div class="row o_kicker_user_row">
                            <t t-if="widget.nightmares">
                                <t t-foreach="widget.nightmares" t-as="player">
                                    <t t-call="PlayerCard">
                                        <t t-set="card_class" t-value="'o_kicker_user_card'"/>
                                    </t>
                                </t>
                            </t>
                            <t t-else="" t-call="PlayerCardLoader">
                                <t t-set="card_class" t-value="'o_kicker_user_card'"/>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>

    <t t-name="Profile">
        <div class="container-fluid o_kicker_main">
            <div t-if="widget.player" class="text-center">
                <div class="text-right mb8 mt8">
                    <div class="btn btn-xs">Update Profile <i class="fa fa-pencil"/></div>
                </div>
                <img t-attf-src="/kicker/avatar/{{ widget.player.id }}" class="img-circle"/>
                <h3><t t-esc="widget.player.name"/></h3>
                <h6>You suck at this. Please stop.</h6>
            </div>
            <div t-else="" class="text-center">
                <div class="text-right mb8 mt8">
                    <div class="btn btn-xs btn-disabled">Update Profile <i class="fa fa-pencil"/></div>
                </div>
                <img src="/kicker/static/img/player.svg" class="img-circle avatar_placeholder" width="128px"/>
                <h3>Loading...</h3>
                <h6>This will only take a moment.</h6>
            </div>
        </div>
    </t>

    <t t-name="Community">
        <div class="container-fluid o_kicker_main">
            <h4>Community</h4>
            <label>You played often with</label>
            <div class="container-fluid">
                <div class="row">
                    <t t-if="widget.usual">
                        <t t-foreach="widget.usual" t-as="user">
                            <t t-call="UserCard">
                                <t t-set="user.fav" t-value="user.name.indexOf('e') !== -1"/>
                                <t t-set="card_class" t-value="'col-xs-6 col-md-3 col-lg-2'"/>
                            </t>
                        </t>
                    </t>
                    <t t-else="" t-call="UserCardLoader">
                        <t t-set="card_class" t-value="'col-xs-6 col-md-3 col-lg-2'"/>
                    </t>
                </div>
            </div>
            <label>You never played against</label>
            <div class="container-fluid">
                <div class="row">
                    <t t-if="widget.rare">
                        <t t-foreach="widget.rare" t-as="user">
                            <t t-call="UserCard">
                                <t t-set="user.fav" t-value="user.name.indexOf('e') !== -1"/>
                                <t t-set="card_class" t-value="'col-xs-6 col-md-3 col-lg-2'"/>
                            </t>
                        </t>
                    </t>
                    <t t-else="" t-call="UserCardLoader">
                        <t t-set="card_class" t-value="'col-xs-6 col-md-3 col-lg-2'"/>
                    </t>
                </div>
            </div>
        </div>
    </t>

    <!--  Components -->
    <t t-name="PlayerCard">
        <div t-attf-class="{{ card_class }}">
            <div class="panel text-center">
                <div class="panel-body">
                    <a t-attf-href="/kicker/community/player/{{ player.id }}" data-router="true" class="o_kicker_user_link">
                        <t t-if="player.hasOwnProperty('fav')">
                            <i t-attf-class="o_kicker_user_fav fa {{ player.fav ? 'fa-heart':'fa-heart-o'}}"/>
                        </t>
                        <p>
                            <img t-if="player" t-attf-src="/kicker/avatar/{{ player.id }}" class="img-circle" width="64px"/>
                            <img t-else="" src="/kicker/static/img/player.svg" class="img-circle" width="64px"/>
                        </p>
                        <p t-esc="player.name"/>
                        <small class="text-muted">Catchy phrase</small>
                    </a>
                </div>
            </div>
        </div>
    </t>

    <t t-name="PlayerCardLoader">
        <div t-attf-class="{{ card_class}}">
            <div class="panel text-center">
                <div class="panel-body">
                    <p>
                        <img src="/kicker/static/img/player.svg" class="img-circle avatar_placeholder" width="64px"/>
                    </p>
                    <p>Loading...</p>
                    <small class="text-muted">Just a sec</small>
                </div>
            </div>
        </div>
    </t>

    <t t-name="AddScore">
        <div class="container-fluid o_kicker_main">
            <form method='post' action='/kicker/score/submit' class="o_kicker_score">
                <h4>Game Location</h4>
                <div class="form-group">
                    <select class="form-control" name="kicker_id">
                        <t t-if="widget.kickers">
                            <t t-foreach="widget.kickers" t-as="kicker">
                                <t t-if="kicker.id === widget.default_kicker">
                                    <option t-att-value="kicker.id" selected="true"><t t-esc="kicker.nickname"/></option>
                                </t>
                                <t t-else="0">
                                    <option t-att-value="kicker.id"><t t-esc="kicker.nickname"/></option>
                                </t>
                            </t>
                        </t>
                    </select>
                </div>
                <h4>Green Team</h4>
                <div class="form-group">
                    <label for="player1">Members</label>
                    <select class="form-control" name="player11">
                        <option value="-1">Select player...</option>
                        <t t-if="widget.players">
                            <t t-foreach="widget.players" t-as="player">
                                <option t-att-value="player.id"><t t-esc="player.name"/></option>
                            </t>
                        </t>
                    </select>
                    <br/>
                    <select class="form-control" name="player12">
                        <option value="-1">Select player...</option>
                        <t t-if="widget.players">
                            <t t-foreach="widget.players" t-as="player">
                                <option t-att-value="player.id"><t t-esc="player.name"/></option>
                            </t>
                        </t>
                    </select>
                </div>
                <div class="form-group">
                    <label for="score1">Score</label>
                    <input type="number" name="score1" value="0" min="0" step="1" class="form-control"/>
                </div>
                <br/>
                <h4>Red Team</h4>
                <div class="form-group">
                    <label for="player1">Members</label>
                    <select class="form-control" name="player21">
                        <option value="-1">Select player...</option>
                        <t t-if="widget.players">
                            <t t-foreach="widget.players" t-as="player">
                                <option t-att-value="player.id"><t t-esc="player.name"/></option>
                            </t>
                        </t>
                    </select>
                    <br/>
                    <select class="form-control" name="player22">
                        <option value="-1">Select player...</option>
                        <t t-if="widget.players">
                            <t t-foreach="widget.players" t-as="player">
                                <option t-att-value="player.id"><t t-esc="player.name"/></option>
                            </t>
                        </t>
                    </select>
                </div>
                <div class="form-group">
                    <label for="score2">Score</label>
                    <input type="number" name="score2" value="0" min="0" step="1" class="form-control"/>
                </div>
                <br/>
                <button type="submit" class="btn btn-lg btn-success">Save</button>
            </form>
        </div>
    </t>

</templates>